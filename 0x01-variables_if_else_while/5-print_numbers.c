#include <stdio.h>
/**
 * main - a program that prints the alphabet in lowercase
 * Return: 0 (Success)
 */
int main(void)
{
	int i = 0;

	while (i < 10)
	{
		printf("%d", i);
		i++;
	}
	return (0);
}
